version: 2

sources:
  - name: jaffle_shop_customers
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null
              